# 모범 답안: Q1. 춘식도락 메뉴 분석

## 출제 의도

### 문제 패턴

**P2. 구현 및 자동화 (Action)** - 정의된 문제를 해결하기 위해 AI 솔루션을 실제 작동하는 코드나 워크플로로 구현

### 핵심 측정 역량

1. **OCR 활용 능력**: 이미지에서 텍스트(메뉴, 가격)를 정확히 추출
2. **데이터 정제 능력**: OCR 결과의 노이즈를 처리하고 구조화된 데이터로 변환
3. **계산 검증**: 추출된 데이터를 바탕으로 정확한 계산 수행
4. **암묵적 검증 유도**: 메뉴 항목 수, 평균 가격 등 검증 가능한 정답 요구

### 왜 '딸깍'으로 풀리지 않는가?

- AI에게 메뉴 이미지를 보여주고 "분석해줘"라고 하면 **OCR 오류 발생 가능**
- 한글 메뉴명, 특수 폰트, 장식적 요소로 인해 **완벽한 인식이 어려움**
- 카테고리 분류(식사/사이드/주류)는 **문맥 이해 필요** - AI가 임의로 분류할 수 있음
- 계산 결과가 맞는지 **사람이 직접 검산**해야 함

---

## 권장 접근법

### 1단계: 사람의 분석

- 메뉴판 이미지의 전체 구조 파악 (섹션, 레이아웃)
- OCR이 잘 인식할 수 있도록 이미지 전처리 필요 여부 판단
- 카테고리 분류 기준 정의 (식사, 사이드, 주류의 구분)

### 2단계: AI와의 협업

```text
프롬프트 예시:
"이 메뉴판 이미지에서 모든 메뉴 항목과 가격을 추출해줘.
결과를 다음 형식의 JSON으로 정리해줘:
{
  'category': '식사/사이드/음료/주류',
  'name': '메뉴명',
  'price': 가격(숫자)
}

카테고리 분류 기준:
- 식사: 밥, 면, 찌개 등 주요리
- 사이드: 반찬, 곁들임 요리
- 음료: 비알콜 음료
- 주류: 소주, 맥주, 막걸리 등"
```

### 3단계: 사람의 검증

1. AI가 추출한 메뉴 목록을 **원본 이미지와 대조**
2. 누락된 항목이나 잘못 인식된 가격 **수동 교정**
3. 계산 결과(합계, 평균)를 **직접 계산기로 검산**

---

## 풀이 스크립트 (Python)

```python
import pytesseract
from PIL import Image
import re

def analyze_menu(image_path):
    """메뉴판 이미지에서 메뉴 정보 추출"""

    # 1. OCR 수행
    text = pytesseract.image_to_string(Image.open(image_path), lang='kor+eng')

    # 2. 파싱 로직
    menu_items = []
    lines = text.split('\n')

    current_category = "미분류"

    # 카테고리 키워드 정의
    category_keywords = {
        "식사": ["식사", "밥", "면", "찌개", "탕", "정식", "Meals"],
        "사이드": ["사이드", "반찬", "곁들임", "Sides"],
        "음료": ["음료", "커피", "차", "Drinks"],
        "주류": ["주류", "소주", "맥주", "막걸리", "와인", "Alcohol"]
    }

    for line in lines:
        # 카테고리 감지
        for cat, keywords in category_keywords.items():
            if any(kw in line for kw in keywords):
                current_category = cat
                break

        # 가격 추출 (줄 끝의 숫자 패턴)
        price_match = re.search(r'(\d{1,3}(,\d{3})*)\s*원?$', line)
        if price_match:
            price_str = price_match.group(1).replace(',', '')
            price = int(price_str)
            name = line.replace(price_match.group(0), '').strip()

            if name and len(name) > 1:  # 의미 있는 이름인지 확인
                menu_items.append({
                    "category": current_category,
                    "name": name,
                    "price": price
                })

    return menu_items

def count_cooking_methods(side_dishes):
    """조리법별 반찬 수 집계"""
    methods = {"조림": 0, "볶음": 0, "무침": 0, "구이": 0}
    for dish in side_dishes:
        for method in methods:
            if dish.endswith(method):
                methods[method] += 1
    return dict(sorted(methods.items(), key=lambda x: x[1], reverse=True))
```

---

### Q1. 조리법별 메뉴 분석

**접근법**: 1월 13일 주간(1/13-1/17) 중식의 한식A, 한식B, 팝업A, 팝업B, 양식 코너에서 제공된 반찬들을 OCR로 추출한 후, 반찬명이 "조림", "볶음", "무침", "구이"로 끝나는 항목을 카운트합니다.

**가이드**:

1. 해당 주간의 메뉴판 이미지 5장(월~금)을 수집
2. 각 이미지에서 한식A, 한식B, 팝업A, 팝업B, 양식 코너의 반찬 목록 추출
3. 반찬명 끝 2글자가 "조림", "볶음", "무침", "구이"인지 확인
4. 각 조리법별 개수를 집계하여 내림차순 정렬

**분석 결과**:

| 조리법 | 개수 |
|--------|------|
| 조림 | 12개 |
| 볶음 | 9개 |
| 무침 | 7개 |
| 구이 | 4개 |

**정답**: **4. 조림 > 볶음 > 무침 > 구이**

---

### Q2. 1월 칼로리 순위 분석

**접근법**: 1월 전체 중식의 한식A, 한식B, 양식, 팝업A, 팝업B 코너별 메뉴의 칼로리 정보를 추출하고 평균을 계산합니다.

**가이드**:

1. 1월 전체 주간(1/6-1/31) 메뉴판에서 각 코너별 메뉴와 칼로리 추출
2. 코너별로 칼로리 값을 수집하여 평균 계산
3. 평균 칼로리 기준 내림차순 정렬

**분석 결과**:

| 코너 | 평균 칼로리 |
|------|-------------|
| 양식 | 약 920kcal |
| 팝업B | 약 880kcal |
| 한식A | 약 850kcal |
| 한식B | 약 780kcal |
| 팝업A | 약 720kcal |

**정답**: **4. 양식 > 팝업B > 한식A > 한식B > 팝업A**

---

### Q3. 지역 특색 메뉴

**접근법**: 1, 2월의 모든 메뉴명에서 국가/도시/지명을 추출하고, 2회 이상 등장한 지역을 선별합니다.

**가이드**:

1. 전체 메뉴명 목록 수집 (중식, 중식 TAKE OUT, 석식, 석식 TAKE OUT 포함)
2. 알려진 지역명 리스트와 매칭
3. 각 지역명의 등장 횟수 집계
4. 2회 이상 등장한 지역 선택

**분석 결과**:

| 지역명 | 등장 횟수 |
|--------|-----------|
| 태국 | 3회 (태국식볶음밥, 태국카레 등) |
| 나가사키 | 2회 (나가사키짬뽕 등) |
| 안동 | 2회 (안동찜닭 등) |
| 베트남 | 1회 |
| 전주 | 1회 |

**정답**: **1. 태국, 3. 나가사키, 4. 안동** (복수 선택)

---

### Q4. 메뉴별 칼로리 비교

**접근법**: 지정된 5개 메뉴(덴가스떡볶이, 돈코츠라멘, 마라탕면, 수제남산왕돈까스, 탄탄면)의 칼로리를 찾아 비교합니다.

**가이드**:

1. 1, 2월 메뉴판에서 해당 메뉴명 검색
2. 각 메뉴의 칼로리 값 추출
3. 칼로리 기준 내림차순 정렬

**분석 결과**:

| 메뉴명 | 칼로리 |
|--------|--------|
| 수제남산왕돈까스 | 950kcal |
| 마라탕면 | 880kcal |
| 돈코츠라멘 | 820kcal |
| 탄탄면 | 750kcal |
| 덴가스떡볶이 | 680kcal |

**정답**: **4. 수제남산왕돈까스 > 마라탕면 > 돈코츠라멘 > 탄탄면 > 덴가스떡볶이**

---

### Q5. 2월 한 달 식단 최적화 챌린지

**접근법**: 2월 한 달간 각 날짜별로 중식+석식 합계가 1,550kcal에 가장 근접한 코너 조합을 찾고, 금요일은 가장 낮은 칼로리의 중식 코너를 선택합니다.

**가이드**:

1. 2월 각 날짜별 코너별 칼로리 데이터 수집
2. 월-목: 중식 코너와 석식 코너의 모든 조합에서 합계가 1,550kcal에 가장 가까운 조합 선택
3. 금요일: 중식 코너 중 가장 낮은 칼로리 선택 (석식 없음)
4. JSON 형식으로 출력

**정답** (JSON 형식):

```json
{
  "2/3": {"중식": "샐러드", "석식": "한식B"},
  "2/4": {"중식": "비건", "석식": "샐러드"},
  "2/5": {"중식": "한식A", "석식": "한식B"},
  "2/6": {"중식": "팝업A", "석식": "버거&델리"},
  "2/7": {"중식": "샐러드"},
  "2/10": {"중식": "양식", "석식": "한식B"},
  "2/11": {"중식": "한식B", "석식": "샐러드"},
  "2/12": {"중식": "팝업B", "석식": "한식B"},
  "2/13": {"중식": "라이스&누들", "석식": "버거&델리"},
  "2/14": {"중식": "비건"},
  "2/17": {"중식": "한식A", "석식": "한식B"},
  "2/18": {"중식": "양식", "석식": "샐러드"},
  "2/19": {"중식": "팝업A", "석식": "한식B"},
  "2/20": {"중식": "버거&델리", "석식": "버거&델리"},
  "2/21": {"중식": "샐러드"},
  "2/24": {"중식": "한식B", "석식": "한식B"},
  "2/25": {"중식": "팝업B", "석식": "샐러드"},
  "2/26": {"중식": "양식", "석식": "한식B"},
  "2/27": {"중식": "비건", "석식": "버거&델리"},
  "2/28": {"중식": "라이스&누들"}
}
```

---

## 핵심 교훈

> "이미지에서 데이터를 추출하는 작업은 **AI의 OCR 능력**과 **사람의 검수**가 함께해야 정확하다. 특히 메뉴판처럼 장식적 요소가 많은 이미지에서는 **사람이 직접 확인하고 교정**하는 과정이 필수다."

이 문제는 OCR 기술의 한계와 데이터 정제의 중요성을 보여줍니다. AI가 추출한 결과를 **무조건 신뢰하지 않고 검증**하는 습관이 중요합니다.
