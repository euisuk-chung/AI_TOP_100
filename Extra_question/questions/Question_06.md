# Question
Agents cannot generalize to new instructions.

# Answer
LLM Agent

# Explanation
Agents fail on new tasks when they rely on fixed rules instead of real reasoning. The LLM Agent setup fixes this by letting the LLM do what it’s best at—general reasoning—while giving it structure, memory, and tools so it can apply that reasoning to follow new instructions with flexibility, not brittle rules.
