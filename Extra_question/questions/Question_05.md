# Question
Agents cannot reason over relationships between retrieved documents.

# Answer
Graph RAG

# Explanation
Agents treat retrieved documents as isolated chunks and miss how they relate. Graph RAG solves this by building a graph of connections— people, places, events—so the agent can reason across the whole network, not just one document at a time.
